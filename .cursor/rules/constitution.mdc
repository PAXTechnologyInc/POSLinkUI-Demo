---
description: Android SDD 通用宪法，定义跨项目的最高层治理原则。
alwaysApply: true
---
# Android SDD 通用宪法

## 核心原则

### 规范优先（Spec-First）

所有功能开发应先明确规格，再形成技术方案与任务拆解；实现必须与规格一致，不得擅自偏离。

### 工具链对齐

- 使用仓库选定的 SDD 工具链管理需求、设计、任务与实现。
- 若项目采用 OpenSpec，则以 `openspec/` 目录和对应命令为准。
- 若项目采用其他 SDD 工具链，则以该项目的目录结构与命令约定为准。

### 测试策略

- 新功能与重要 Bug 修复应先补充失败测试或明确验收场景，再进入实现。
- 关键业务逻辑（UseCase、Repository、ViewModel、Parser、Mapper 等）应具备可验证测试。
- 优先使用可控 Fake 与集成测试，减少无收益的过度 Mock。

### 简洁与可维护

- 只实现规格明确要求的能力，避免过度设计。
- 优先官方或成熟方案；新增依赖需说明收益、代价和替代方案。
- 模块、包、类、函数保持单一职责，高内聚低耦合。
- 禁止以全局可变状态隐式传递核心业务状态；依赖应显式注入或显式提供。

### 线程与性能

- IO、网络、数据库、序列化、加解密等工作不得阻塞主线程。
- 生命周期结束时必须释放订阅、监听、回调和系统资源。
- 性能优化应基于真实瓶颈，避免无依据的复杂化设计。

### 安全编码

- 禁止硬编码敏感信息（密码、Token、密钥、证书私钥）。
- 日志必须脱敏，异常输出不得暴露内部路径、密钥或敏感载荷。
- 网络通信默认使用 HTTPS，禁止关闭证书校验。
- 外部输入必须校验；数据库访问必须使用安全参数化机制。
- 语言级安全规则见 `.cursor/rules/` 下的安全规范文件。

## 开发流程

- 新需求：Constitution → Spec → Design/Plan → Tasks → Implement。
- Bug 修复：先定位是否为规格缺失或实现偏差；必要时补充规格或任务后再修改代码。
- 实施前应优先阅读当前feature的规格、设计文档、任务文档和项目专属规则。

## 协作与交付

- 提交前应通过代码格式化、单元测试、Lint 或等效静态检查。
- 代码审查时需同时核对：是否符合宪法、是否符合实施细则、是否符合当前feature规格。
- README应包含启动、构建、测试和必要的环境说明。
- 生产环境禁止保留调试后门、测试凭证、Mock开关或占位资源。
