---
description: Java 语言安全编码规范（基于 CERT Java 安全编码标准）
globs: "**/*.java"
alwaysApply: false
---

# Java 语言安全规则

## 1. 输入验证和数据过滤

### J-01：禁止日志记录未经过滤的用户输入
- **原因**：防止日志注入漏洞，避免恶意用户插入虚假日志数据。
- **不符合示例**：
  ```java
  logger.severe("User login failed for: " + username);
  ```
- **符合示例**：
  ```java
  logger.severe("User login failed for: " + sanitizeUser(username));
  ```

### J-02：传递给Runtime.exec()的数据必须进行过滤
- **原因**：防止命令和参数注入攻击。

### J-03：防止SQL/XML/JSON注入
- **原因**：防止恶意数据篡改查询或破坏数据结构。
- **防护措施**：使用参数化查询防止SQL注入；严格XML输入验证；JSON特殊字符转义。

## 2. 声明和初始化

### J-04：防止类初始化循环
- **原因**：避免类初始化循环导致不可预测的行为。静态字段声明顺序需保证依赖在前。

### J-05：不要使用Java标准库的公共标识符
- **原因**：避免遮蔽标准库中的同名类，导致程序行为异常。

### J-06：在增强型for语句中不要修改循环变量
- **原因**：增强型for循环的循环变量是局部变量，修改它不会影响集合本身。

## 3. 表达式

### J-07：禁止忽略方法返回的值
- **原因**：方法可能通过返回值传递状态或更新对象。如 `String.replace()` 返回新字符串。

### J-08：禁止使用Object.equals()比较数组
- **原因**：数组的equals比较的是引用而非内容，应使用 `Arrays.equals()`。

### J-09：禁止使用==比较封装的基本类型
- **原因**：包装类使用==比较的是对象引用而非实际值，应使用 `.equals()`。

## 4. 数字类型和操作

### J-10：精确计算时禁止使用浮点数
- **原因**：浮点数存在精度限制。使用整数或 BigDecimal。

### J-11：禁止与NaN进行直接比较
- **原因**：NaN具有无序性，应使用 `Double.isNaN()`。

### J-12：禁止使用浮点变量作为循环计数器
- **原因**：浮点变量存在精度丢失和递增失效的问题。

### J-13：浮点数输入检查异常值
- **原因**：应使用 `Double.isInfinite()` 和 `Double.isNaN()` 检查异常值。

## 5. 字符和字符串

### J-14：禁止将非字符数据编码为字符数据
- **原因**：非字符数据编码为字符串可能导致数据完整性丢失。

### J-15：JVM间通信使用兼容字符编码
- **原因**：明确指定 `Charset`（如 `UTF-8`），避免编码不匹配。

## 6. 对象

### J-16：禁止复制敏感类
- **原因**：防止攻击者通过克隆绕过安全检查。敏感类应声明为 `final`。

### J-17：禁止从嵌套类暴露外部类的私有成员
- **原因**：防止封装性被破坏，避免信息泄露。

### J-18：不采用比较类名来判断类是否相同
- **原因**：应使用 `getClass() == Xxx.class`，防止恶意类绕过安全检查。

### J-19：谨慎在构造函数中抛出异常
- **原因**：防止部分初始化对象被利用，敏感类应声明为 `final`。

## 7. 方法

### J-20：禁止使用assert验证方法参数
- **原因**：assert可能被禁用，应使用 `if` + `throw IllegalArgumentException`。

### J-21：安全检查方法必须声明为private或final
- **原因**：防止恶意子类覆盖以绕过安全验证。

### J-22：构造函数禁止调用可重写的方法
- **原因**：防止未初始化数据使用或this引用泄露。

### J-23：验证方法参数
- **原因**：null 参数应抛出 `IllegalArgumentException`。

### J-24：不使用finalizers
- **原因**：finalize()可能导致资源释放不可靠、对象复活等问题。

## 8. 异常

### J-25：禁止抑制或忽略异常
- **原因**：不得空 catch 或仅 `e.printStackTrace()`。

### J-26：禁止异常暴露敏感信息
- **原因**：防止通过异常消息泄露系统内部结构或用户数据。

### J-27：禁止在finally块中终止执行
- **原因**：finally块不得通过return、break、continue提前终止。

### J-28：不抛出RuntimeException/Exception/Throwable
- **原因**：应使用具体异常类型，便于调用方精准处理。

## 9. 锁与线程

### J-29：同步过程中避免死锁
- **原因**：不要对 interned 字符串加锁；使用私有 final Object 作为锁对象。

### J-30：确保异常时释放持有的锁
- **原因**：Lock 必须在 finally 块中 unlock。

### J-31：禁止调用Thread.run()新建线程
- **原因**：应使用 `Thread.start()`。

### J-32：禁止调用Thread.stop()终止线程
- **原因**：应使用 `Thread.interrupt()`。

### J-33：禁止调用ThreadGroup方法
- **原因**：应使用 `ExecutorService` 等现代并发工具替代。

## 10. 输入和输出

### J-34：禁止将缓冲区暴露至外部
- **原因**：应返回 `.asReadOnlyBuffer()`。

### J-35：创建文件时设置适当访问权限
- **原因**：防止未授权访问。

### J-36：及时清除临时文件
- **原因**：防止敏感数据残留，使用 `DELETE_ON_CLOSE`。

## 11. 序列化

### J-37：禁止序列化明文敏感数据
- **原因**：敏感字段应标记为 `transient`。

### J-38：谨慎反序列化不受信任的数据
- **原因**：应使用白名单机制 `resolveClass()` 过滤。

## 12. 平台安全与其他

### J-39：禁止在特权代码中直接使用外部数据
- **原因**：外部数据必须先清理/验证再传入特权上下文。

### J-40：不使用反射增加可访问性
- **原因**：防止破坏封装性。

### J-41：禁用java.util.Random产生安全用途的伪随机数
- **原因**：安全场景应使用 `java.security.SecureRandom`。

### J-42：禁止硬编码敏感信息
- **原因**：防止反编译提取明文密码、密钥、连接串。

### J-43：数据交换时建议使用SSLSocket
- **原因**：提供 TLS 完整性保护与双向认证。

### J-44：生产代码禁止包含调试入口点
- **原因**：防止未预期的交互模式，形成安全风险。
