---
description: 项目背景与重构目标：源自 golive 分支 Java 版 PosLinkUI Demo，本仓库以 Kotlin + Compose 等技术栈重写，页面与功能需与旧版一致。始终生效，供新会话与实现决策对齐基准。
alwaysApply: true
---

# 项目背景（POSLinkUI-Demo 重构）

## 来源与定位

- **上游基线**：本仓库由 **Git 分支 `golive/v1.03.00`**（口语中常称 golive1.03）上的 **Java 版 PosLinkUI Demo** 代码演进、重构而来。
- **技术栈迁移**：目标采用 **Kotlin**、**Jetpack Compose** 等与当前 Android 官方推荐一致的新技术栈实现同等产品能力；与旧版共存的少量 Java（例如个别自定义 View 或过渡代码）应视为迁移过程中的例外，并朝 Kotlin/Compose 收敛。

## 成功标准

- **功能对等**：各入口、页面流程、参数与行为应与 **Java / XML 版 Demo** 在相同业务场景下表现一致，不得无故删减或改变对外可见行为。
- **体验与规格对齐**：UI 尺寸、文案与交互细节以 golive 权威资源为准；具体还原方法与回归验证见 `.cursor/skills/compose-ui-parity-golive-posuinew/SKILL.md` 及规格文档 `specs/` 中相关说明。

## 协作提示

- 实现新页面或调整现有页面时，应明确对照 **golive 分支上的 layout/dimens/逻辑**，避免仅凭主观重设计偏离基线。
- 若旧版行为存在歧义，应优先在规格或任务文档中澄清后再改代码。

## 对照旧版代码时的权威来源（必读）

- **以 GitLab 远程为准**：做行为/UI 对齐、与 Java 版逐行对照时，应以 **GitLab 上团队约定的 golive 线分支**（如 **`golive/v1.03.00`**，口语 **golive1.03**）为**权威基线**，在远程上查看或 `git fetch` 后检出对应提交再对照。
- **不要默认使用本机路径**：开发者本机其他目录（例如另一份克隆的 `POSLinkUI-Demo`）**仅作辅助**，可能与远程分支不一致；除非已确认与 **远程 golive 分支** 提交一致，否则不得把它当作唯一真相源。
- **可操作方式**：在本地为权威远程分支建跟踪分支、用 `git show <remote>/<branch>:path` 取文件，或通过 GitLab Web 浏览指定分支下的文件。

## 工作区范围说明

- **同级目录不在本仓库内**：例如 **`bugfix/`** 等文件夹若位于**与本项目仓库根目录同级**的路径下，则**不在当前 Cursor workspace 内**，助手默认**无法读取**；需要你在对话里给出绝对/相对路径、将文件拷入本仓库，或把该目录加入 workspace，才能对照其中的草稿（如 Untitled）。
